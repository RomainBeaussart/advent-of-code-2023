export type FindedNumber = {
    number: number,
    x: number,
    y: number
    lenght: number
    partNumbers: { x: number, y: number }[]
}

export type FindedGear = {
    x: number,
    y: number
    partNumbers: number[]
}